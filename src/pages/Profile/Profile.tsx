import { FC } from 'react';

import { ResetPasswordForm, FormComponent } from 'components';

export const Profile: FC = () => {
  return (
    <FormComponent>
      <ResetPasswordForm />
    </FormComponent>
  );
};
