import { FC } from 'react';

import { RegisterForm, FormComponent } from 'components';

export const Register: FC = () => {
  return (
    <FormComponent>
      <RegisterForm />
    </FormComponent>
  );
};
